# Enable mouse support
set -g mouse on

set-option -sg escape-time 10
set-option -g focus-events on

# Set terminal color support
set-option -g default-terminal "screen-256color"
setw -g xterm-keys off

# Prefix key configuration
set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

# Split screen bindings
unbind %
bind | split-window -h -c "#{pane_current_path}"
unbind '"'
bind - split-window -v -c "#{pane_current_path}"

# Resize pane bindings
bind -r j resize-pane -D 2
bind -r k resize-pane -U 2
bind -r h resize-pane -L 2
bind -r l resize-pane -R 2

# Move pane
bind -r J swap-pane -D
bind -r K swap-pane -U
bind -r H swap-pane -s '{ left-of }'
bind -r L swap-pane -s '{ right-of }'

# Switch panes
bind -r C-h select-pane -L
bind -r C-j select-pane -D
bind -r C-k select-pane -U
bind -r C-l select-pane -R

# Switch windows
bind -r C-S-h previous-window
bind -r C-S-l next-window

# Open in current path
bind c new-window -c "#{pane_current_path}"

# Rename
bind C-S command-prompt -p "Rename window to:" rename-window

# Reload tmux configuration
unbind r
bind r source-file ~/.config/tmux/tmux.conf

# Copy to clipboard
# For macOS
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy -selection clipboard -i"

# For Arch/Linux
#bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -selection clipboard -i"

# VS Code Dark+ theme
set -g mode-style bg=colour24,fg=colour231

# Status bar
set -g status-bg colour233
set -g status-fg colour231
set -g status-interval 1
set -g status-left-length 50
set -g status-right-length 100

# Status left (session)
set -g status-left "#[bg=colour24,fg=colour231,bold] #S #[bg=colour233,fg=colour24]"

# Status right (date/time)
set -g status-right "#[fg=colour24]#[bg=colour24,fg=colour231] %H:%M #[bg=colour67,fg=colour231,bold] %d %b %Y "

# Window status
set -g window-status-format "#[fg=colour244] #I:#W "
set -g window-status-current-format "#[bg=colour24,fg=colour231,bold] #I:#W #[bg=colour233,fg=colour24]"

# Pane borders
set -g pane-border-style fg=colour240
set -g pane-active-border-style fg=colour24

# Message style
set -g message-style bg=colour24,fg=colour231
set -g message-command-style bg=colour24,fg=colour231

# Clock mode
set -g clock-mode-colour colour24

# Suppress bell
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
set -g monitor-activity off
set -g bell-action none

set-option -g status "on"

# tpm
set -g @plugin 'tmux-plugins/tpm'

# Run TPM
run '/opt/homebrew/opt/tpm/share/tpm/tpm'
